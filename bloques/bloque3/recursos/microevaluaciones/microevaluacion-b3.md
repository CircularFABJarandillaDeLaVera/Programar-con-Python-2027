# Microevaluaciones - Bloque 3

Formato: respuesta rapida, feedback inmediato y vuelta al codigo.

1. Predecir que devuelve una funcion con `return`.
2. Distinguir `print()` de `return`.
3. Completar un parametro por defecto.
4. Identificar parametro y argumento.
5. Detectar una variable local frente a una exterior.
6. Elegir cuando aparece `global`.
7. Colocar un docstring.
8. Completar `except ValueError`.
9. Interpretar `else` en try/except.
10. Elegir modo `"a"` para anadir al final.
11. Usar `json` para guardar diccionarios.
12. Importar una funcion desde otro modulo.

Todas las preguntas estan cargadas en `data/bloque3.js`.
